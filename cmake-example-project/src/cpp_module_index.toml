version = 1

[modules."interop::host"]
cpp_module = "interop.host"
namespace = "interop::api"

[modules."interop::host".symbols."Counter::add"]
kind = "method"
callable_signatures = ["int(int)"]

[modules."interop::host".symbols.apply_bias]
kind = "function"
callable_signatures = ["int(int)"]
