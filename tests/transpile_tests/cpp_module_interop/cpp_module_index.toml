version = 1

[modules.std]
cpp_module = "std"
namespace = "std"

[modules.std.symbols.max]
kind = "function"
callable_signatures = ["int(int,int)"]

[modules."custom::math"]
cpp_module = "custom.math"
namespace = "custom::math"

[modules."custom::math".symbols.add_one]
kind = "function"
callable_signatures = ["int(int)"]

[modules."custom::math".symbols.DEFAULT_BIAS]
kind = "constant"
